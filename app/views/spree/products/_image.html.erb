<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:large), itemprop: "image", class: "center-block img-fluid" %>
<% else %>
  <%= image_tag @product.display_image.attachment(:large), itemprop: "image", class: "center-block img-fluid" %>
<% end %>

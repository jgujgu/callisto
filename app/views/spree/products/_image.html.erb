<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), itemprop: "image", class: "center-block" %>
<% else %>
  <%= image_tag @product.display_image.attachment(:product), itemprop: "image", class: "center-block" %>
<% end %>
